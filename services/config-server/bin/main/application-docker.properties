spring.application.name=config-server

server.port=8888

spring.cloud.config.server.git.uri=https://github.com/Disablek/config-server-onlineCourses_project-.git
spring.cloud.config.server.git.default-label=main

# Disable Eureka client registration - config-server must start independently
# before Eureka can start (Eureka depends on config-server health)
eureka.client.enabled=false
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

# Prevent config server from acting as a Config Client for itself
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false

management.endpoints.web.exposure.include=health,info
